PREFERRED_VERSION_virtual/${TARGET_PREFIX}golang ?= "${GOLANGVERSION}"
PREFERRED_VERSION_golang-cross-${TARGET_ARCH} ?= "${GOLANGVERSION}"
PREFERRED_VERSION_golang ?= "${GOLANGVERSION}"
PREFERRED_VERSION_golang-runtime ?= "${GOLANGVERSION}"
PREFERRED_VERSION_golang-cross-canadian-${TRANSLATED_TARGET_ARCH} ?= "${GOLANGVERSION}"
PREFERRED_VERSION_golang-crosssdk-${SDK_ARCH} ?= "${GOLANGVERSION}"
PREFERRED_VERSION_golang-native ?= "${GOLANGVERSION}"
PREFERRED_VERSION_nativesdk-golang ?= "${GOLANGVERSION}"
PREFERRED_VERSION_nativesdk-golang-runtime ?= "${GOLANGVERSION}"

